package Task7;

import java.lang.reflect.Array;
import java.util.Arrays;

public class Main {
    /*Task 7
    Implement a generic method for binary search in the array.*/
    /*public static <T extends Number> int search(int searched,T... numbers) {
        if (numbers == null || numbers.length == 0) return (Number[]) Array.newInstance(numbers.getClass(), 0);
        return Arrays.binarySearch(numbers, searched);
    }

    public static <T extends Number> Number[] sortThem(T... numbers) {
        if (numbers == null || numbers.length == 0) return null;

        Arrays.sort(numbers);
        return numbers;
    }

    public static void main(String[] args) {
        System.out.println(Arrays.toString(sortThem(6, 8, 12, 30, 16, 18, 22)));
        System.out.println(search(new Integer[]{6, 8, 12, 30, 16, 18, 22}, 30);

    }*/
}
